class Params {
    static snake = {
        initialSize: 35,
        color: 'lime',
        speed: 5,
        increasePerFood: 1
    };

    static food = {
        size: 30,
        icons: ['fedora', 'windows', 'archlinux','redhat','ubuntu', 'macos'],
    }
}